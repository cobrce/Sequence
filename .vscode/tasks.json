{
    "tasks": [
        {
            "type": "shell",
            "label": "avr-g++ build active file",
            "command": "avr-g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "."
            }
        }
    ],
    "version": "2.0.0"
}